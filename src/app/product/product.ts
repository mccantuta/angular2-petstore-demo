export class Product {
    constructor(
        public id: number,
        public categoryid: number,
        public categoryname: string,
        public name: string,
        public description: string
    ) {}
}